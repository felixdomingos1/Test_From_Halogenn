export interface IDataDTO {
   nome: string;
   valor: number;
   nota: string;
 }
export type CustomErrorType = {
  message: any;
  // Outras propriedades opcionais do erro podem ser adicionadas aqui
  // Por exemplo:
  // errorCode?: number;
};
